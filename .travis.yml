language: cpp
sudo: required
dist: trusty
compiler:
- gcc
os:
- linux
addons:
apt:
packages:
- cmake
before_install:
- sudo apt-get -y install libopencv-dev
- sudo apt-get -y install libboost-all-dev
before_script:
- cd src/mozjpeg && cmake . && cd ../../
- cmake .
script: 
- make
after_success:
branches:
  only:
  - master
notifications:
env:
  global:
